{
  "name": "dotgui-core",
  "version": "1.0.0",
  "description": "Canonical spec and type definitions for the .gui format",
  "main": "./schema/types.ts",
  "types": "./schema/types.ts",
  "exports": {
    ".": "./schema/types.ts",
    "./types": "./schema/types.ts",
    "./spec": "./spec/spec.json"
  },
  "files": [
    "schema/",
    "spec/",
    "examples/"
  ],
  "scripts": {
    "gen:docs": "bun run scripts/gen-docs.ts",
    "gen:docs:check": "bun run scripts/gen-docs.ts && git diff --exit-code spec/REFERENCE.md"
  }
}
